exception_details-exercise.py:
    Lesson  25
    page 226
    Intentionally create different types of exceptions using the following template:
        try:
            #code that creates exception
        except Exception as e:
             print(type(e))
             print(e, \n)
    See instructions in book for additional details


running_sum-exercise.py:
    Lesson 26
    page 235
    convert a function:
       from using the if/else as flow control
       to using the try/except as flow control
   the code should continue to work in exactly the same way

