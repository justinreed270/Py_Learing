# adding one loop into another
# one application could be to generate a list of coordinates:
#example:
    #(x,y)
    #(0,0)
    #(0,1)
    #(1,1)

for x in range(4):
    for y in range(3):
        print(f'({x},{y})')

